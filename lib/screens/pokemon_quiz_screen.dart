import 'package:flutter/material.dart';

class PokemonQuizScreen extends StatelessWidget {
  const PokemonQuizScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("クイズ画面"),
    );
  }
}
